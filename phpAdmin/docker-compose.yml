services:
  phpmyadmin:
    image: phpmyadmin
    restart: unless-stopped
    ports:
      - 8050:80
    environment:
      PMA_HOSTS: mysql,mariadb
      PMA_PORT: 3306
      UPLOAD_LIMIT: 64M
    networks:
      - shared-net

networks:
  shared-net:
    external: true
